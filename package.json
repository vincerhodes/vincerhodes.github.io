{
  "name": "rightcourtsc",
  "private": true,
  "version": "0.0.0",
  "description": "Right Court SC — club website. web/ holds the Next.js app (Vercel + Turso) serving rightcourtsc.com since 2026-07-19 — see planning/08-VERCEL-MIGRATION.md. The static site at repo root is legacy (kept for history/rollback). Root dev-dependencies are the static site's verify gate.",
  "type": "module",
  "scripts": {
    "test": "npm --prefix web test"
  },
  "devDependencies": {
    "html-validate": "^8.24.0",
    "@playwright/test": "^1.48.0",
    "ajv": "^8.17.1",
    "vitest": "^2.1.4",
    "lighthouse": "^12.2.1",
    "serve": "^14.2.4"
  }
}
